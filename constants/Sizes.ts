import { Dimensions } from "react-native";

const TITLE = 28;
const SUBTITLE = 20;
const TEXT = 14;

const SPACING = 4;

const VH = Dimensions.get("window").height;
const VW = Dimensions.get("window").width;

export { TITLE, SUBTITLE, TEXT, SPACING, VH, VW };
